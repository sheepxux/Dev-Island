# Dev Island Data Flow Inventory

Last engineering review: 2026-08-29

This is the source-backed inventory used to keep product copy, privacy notices,
security review, and release decisions aligned with the application that is
actually built. It is not a claim that every optional flow is enabled.

| Flow | Data | Destination | Persistence | User control | Current release state |
| --- | --- | --- | --- | --- | --- |
| Shared local Agent Hook normalization | Every local Hook identity uses a nonempty, control-free session ID capped at 256 UTF-8 bytes; optional generation ID is capped at 256 bytes; cwd must be an absolute, control-free path capped at 4,096 bytes; phase is capped at 256 characters / 1,024 bytes and message at 1,024 characters / 4,096 bytes | Per-Agent decoder to the shared in-process connector after loopback receipt | Each connector retains at most 128 live sessions in memory. Capacity removes terminal rows, then the oldest Running row; when all 128 are Waiting, the newest arrival is rejected so earlier human-blocking work remains visible | Disabling a connector or **Disconnect All…** stops future managed delivery; normal terminal/stale TTL cleanup still applies | Available; malformed identity is rejected, display text truncates only on complete grapheme boundaries, and deterministic arrival ordering governs ties |
| Local Agent Hook request authorization | A fixed non-secret protocol marker `X-Dev-Island-Hook: v1` plus a per-listener 256-bit random `X-Dev-Island-Authorization` value; neither contains user, task, path, account or Agent content | Numeric `127.0.0.1:7824` only. Every `/hooks/<source>` POST must omit `Origin`, carry the marker, and match the current random value; the listener emits no CORS authorization | The random Header line is atomically rotated at `~/Library/Application Support/island-app/local-hook-authorization.header`, capped at 128 bytes and forced to current-user `0600`; it never enters Agent config, plugin source, argv, logs, SQLite or diagnostics. The fixed marker/path exist in managed commands/plugins | Connector Update rewrites stale managed definitions; disabling removes only Dev Island-owned entries. Missing/unsafe credential storage leaves the listener unavailable and Agent turns fail open to native behavior | Available. The fixed marker blocks browser simple requests; the private credential blocks other macOS users from forging writes. Current-user processes that can read the owner-only file remain inside the explicit local-user trust boundary |
| Managed local Hook configuration | At most 4 MiB of an Agent-owned JSON/TOML configuration, or 256 KiB of the complete Dev-Island-owned OpenCode plugin, is held in memory while parsing and preparing an explicit edit; only exact managed markers/commands/blocks are changed | The same on-device Agent configuration path selected by its reviewed descriptor; no network destination | The Agent owns the resulting file. Writes resolve a safe configuration-directory symlink once, then use an anchored concrete parent descriptor, private same-directory staging, file + directory `fsync`, atomic rename, and exact snapshot comparison; new files are `0600`, existing permissions survive | Per-Agent Enable/Update/Disable and confirmed **Disconnect All…** only; read-only status checks never repair files | Available; target symlink, hard-link, non-regular, wrong-owner, dangling/unsafe-parent, oversized, and detected concurrent-change paths fail closed, while an unsafe linked-file marker remains visible as requiring review |
| Local Agent configuration presentation I/O | Up to the existing bounded Agent-owned configuration input is read only inside a detached worker; only low-cardinality `checking` / `absent` / `current` / `configured` / `update-required` / `disconnected` states, target success/failure sets, disconnected count, or fixed product error copy return to UI. Configuration bytes, file paths, parser errors and unrelated Hook content never cross to the main actor | On-device Settings and Welcome workers only. Single/all-Agent inspection, Enable/Update/Disable, and Settings **Disconnect All…** use the same testable executor; Welcome may also run the existing bounded on-device Codex trust probe while resolving its final snapshot. There is no network destination | No new persistence. A successful mutation still persists only the exact managed Hook edit in the Agent-owned file under the existing atomic descriptor/fsync boundary. UI operation IDs, completion generations, working-source sets and bulk state are memory-only. Settings keeps its operation owner only for the lifetime of the Settings window, above pane reconstruction | Explicit Settings/Welcome action for mutations; opening either surface performs read-only scans. Refresh is latest-wins. Each surface permits one mutation, and Settings uses one owner across every pane, so leaving and returning to Agent Connections cannot re-enable a competing write. View departure invalidates only UI delivery rather than interrupting an atomic write | Available. Both surfaces re-read actual state after mutation; Welcome accepts `configured` only for a documented vendor trust gate and never infers Connected from a returned write. Settings completion must match both operation identity and mutation kind before releasing ownership, then one generation refreshes all rows and summaries. Late scans, departed views and immediately superseded writes cannot overwrite newer UI state; MainActor-to-non-main execution is covered at the shared executor boundary |
| Claude Code / Codex / Cursor lifecycle hooks | Session ID, event type, title/project hints, state; terminal connectors also add bounded terminal bundle/program, TTY and tmux socket/pane hints | `127.0.0.1:7824` | Normalized task row in local SQLite; terminal/tmux jump context remains in live memory only | Enable/disable per connector or **Disconnect All…**; only managed command handlers are removed, with cross-file rollback that preserves concurrent edits; Codex separately requires review or confirmation in `/hooks` | Available; Codex file state is **Configured** until the read-only trust probe verifies activation |
| Local terminal/tmux jump | A validated in-memory terminal bundle hint and, when present, bounded absolute tmux socket path plus numeric pane ID; the first tmux command returns only a window ID | The already-running local terminal app and a validated root/current-user-owned, group/other-nonwritable tmux executable at a reviewed package-manager path; no shell, network, Accessibility, or Apple Events destination | No persistence. tmux stdout is drained while running, capped at 4 KiB, parsed in memory, and then released; HOME and unrelated parent environment values are not inherited | Only an explicit click on a live task. Failure activates the captured host app without precise pane selection | Available; each query/select command has a two-second monotonic deadline and an isolated process group, with output overflow, timeout, hang, or ignored TERM closed by TERM→KILL |
| Codex Hook trust verification | Bounded `hooks/list` metadata may transiently contain all discovered Hook events, commands, source paths, enabled state, definition hashes, trust state, warnings, and errors; only whether every exact Dev Island definition is enabled and `trusted`/`managed` leaves the probe | On-device stdio to a short-lived `codex app-server --stdio` binary embedded in an OpenAI-signed `com.openai.codex` app; no network | Raw stdout is capped at 2 MiB, held only in memory, never logged or copied to diagnostics, and best-effort erased after the check; stderr is discarded | Automatic when a configured Codex status is shown; **Check again** is available in Settings; the probe cannot write config, change trust, or start a thread | Available; timeout, unavailable binary, schema drift, unrelated errors, disabled/untrusted/modified/mismatched definitions all remain **Configured** |
| Local live-readiness preflight | Executable presence and at most 4 KiB of `--version` stdout for the known Claude Code path and OpenAI-signed Codex binary; managed Hook state; Codex trust result; random listener challenge | Explicit `IslandCoreCLI local-live-readiness`; short-lived on-device subprocesses and `127.0.0.1:7824`; no Dev Island remote destination | Raw version output is memory-only, never printed/logged/persisted, and erased after exact token comparison; listener response is an ephemeral UUID echo; only low-cardinality states leave the probe | User explicitly runs the command; it never installs/updates Hooks, changes Codex trust, opens a session, or starts the App | Available as an engineering preflight; exact reviewed versions are Claude Code `2.1.197` and Codex `0.149.0-alpha.4.3`; completed output drift requires review, while spawn/timeout/overflow/nonzero-exit is reported only as `check-failed`; real sessions remain a human gate |
| Hermetic local listener transport check | One in-memory 256-bit random authorization value, one random free loopback port, internal readiness token and external UUID challenge; no Agent descriptor, Hook body, task, path, config or credential is accepted | Explicit `IslandCoreCLI local-hermetic-listener-check`; a short-lived listener on a random numeric `127.0.0.1` port; no Agent or remote destination | No persistence. The authorization value never enters the production Header file; framework logs are suppressed; stdout is exactly five low-cardinality lines and stderr is empty | Maintainer explicitly runs the command or the authoritative CI wrapper runs ten rounds. The server is stopped before success and the challenge route must then be unavailable | Available as an engineering transport fixture only. It proves start/challenge/stop without entering TaskStore, SQLite, Keychain or managed Hook maintenance; it does not claim that the production App or a real Agent session is ready |
| Gemini CLI lifecycle hooks | Gemini's complete stdin payload may transit loopback, including session ID, cwd, event, prompt/response, transcript path, notification message/details; managed Hook also adds bounded terminal/tmux jump hints | `127.0.0.1:7824`; never the public Manus tunnel | Raw body is not retained or logged; normalized task row may enter local SQLite; jump context remains in live memory only | Enable/disable connector or use **Disconnect All…**; uninstall removes only Dev Island `/hooks/gemini-cli` command handlers | Preview: implementation + simulation verified, including jump-context transport; real CLI acceptance pending |
| Qwen Code lifecycle and permission hooks | Complete forward-extensible event body may transit loopback, including session/cwd/transcript metadata, prompts or model content, tool input, permission suggestions, assistant output, failure details and future fields; managed command adds bounded terminal/tmux hints | `127.0.0.1:7824`; never the public Manus tunnel | Raw body is not retained or logged; bounded permission detail is in memory while pending; normalized task row may enter SQLite; jump context stays in live memory | Enable/disable or **Disconnect All…**; uninstall removes only `/hooks/qwen-code` handlers; Allow/Deny or neutral fallback per request | Preview: fixtures, config safety and loopback response verified; real `0.22.0` CLI acceptance pending |
| GitHub Copilot CLI lifecycle and attention hooks | Complete forward-extensible compatible Hook body may transit loopback, including session/cwd, prompt/transcript, notification title/message, error message/stack, tool data and future fields; managed command adds bounded terminal/tmux hints | `127.0.0.1:7824`; never the public Manus tunnel | Raw body and vendor-authored text are not retained or logged; only event/category/recoverability plus fixed waiting copy can enter normalized task state/SQLite; jump context stays live-memory-only | Enable/disable or **Disconnect All…**; Dev Island owns only `~/.copilot/hooks/dev-island.json` commands containing `/hooks/copilot-cli`; all approval/input responses stay in Copilot CLI | Preview: schema pin, config safety and real managed-command loopback verified; real `1.0.80` CLI acceptance pending |
| Kimi Code CLI lifecycle and approval-attention hooks | Complete forward-extensible Hook body may transit loopback, including session/title/client/cwd, turn origin and prompt, tool name/input/display, approval result/feedback, error message and future fields; managed command adds bounded terminal/tmux hints | `127.0.0.1:7824`; never the public Manus tunnel | Raw body and vendor-authored text are not retained or logged; only event/session/cwd plus an allowlisted error category and fixed waiting copy can enter normalized task state/SQLite; jump context stays live-memory-only | Enable/disable or **Disconnect All…**; Dev Island edits only explicit `/hooks/kimi-code` managed blocks in `~/.kimi-code/config.toml`; Kimi's native UI remains the only approval surface | Preview: `0.38.0` default-engine contract, lossless TOML fixtures, cross-format rollback and real managed-command loopback verified; real signed-in CLI acceptance pending |
| OpenCode lifecycle and permission-attention plugin | The plugin allowlists schema version, seven event categories, session ID, cwd, and `busy` / `idle` / `retry`; bounded terminal bundle/program and tmux hints travel as headers. Title, prompt, messages, tool arguments, permission metadata and raw error content are filtered before transit | Unawaited POST to `127.0.0.1:7824` with one-second abort; never the public Manus tunnel | Normalized task row may enter SQLite; jump context stays live-memory-only; raw vendor Event is never sent to Dev Island | Enable/disable or **Disconnect All…**; Dev Island owns only the complete marked `~/.config/opencode/plugins/dev-island.js`, rejects unsafe/unowned collisions and leaves all approval responses in OpenCode | Preview: pinned `1.18.23` fixture, privacy, `0600` ownership and rollback verified; real CLI discovery/reload/timing/fallback pending; `permission.ask` output deliberately unused |
| Codex / Claude / Qwen permission request | Session ID, tool name, reason, bounded rendered input; Allow/Deny response. Title/message/detail are capped at 256/1,024/4,096 visible characters and 1/4/16 KiB UTF-8 | In-process UI through loopback Hook | Pending details in memory; normalized Waiting state may be stored. All synchronous action types share a 32-request global and 4-request-per-`(source,sessionId)` memory queue; default lifetime is 90 seconds and absolute lifetime is 120 seconds | User must press Allow/Deny or let it fall back. Expiry, cancellation, duplicate ID or queue overflow returns neutral immediately to the Agent-native prompt without retaining a continuation | Codex/Claude implemented locally; Qwen remains Preview; real UI gates pending |
| Claude `AskUserQuestion` | Up to 4 questions with up to 8 options each, option labels/descriptions and selected answers. Question/header/label/description are bounded by both visible characters and 2,048/256/512/2,048 UTF-8 bytes | In-process UI; documented response returned to Claude Hook | Pending form uses the shared bounded action queue in memory; normalized Waiting state may be stored | Submit or **Continue in Claude** fallback; text is truncated only at complete grapheme boundaries, while malformed, empty or duplicate values fail neutral | Implemented locally; combining-grapheme and queue-cap regressions verified; real vendor/UI gate pending |
| Claude `ExitPlanMode` review | Session ID, injected plan Markdown capped at 65,536 characters **and** 262,144 UTF-8 bytes, plan path and complete injected tool input capped at 262,144 bytes; Approve/Reject response | In-process UI. Block and inline Markdown parsing runs once per request in an on-device detached worker; only an immutable document of at most 512 complete blocks returns to SwiftUI. The documented response returns to the Claude Hook; there is no Dev Island network destination | Full plan/input uses the shared bounded action queue and is held in memory only while pending; rendered blocks, request/operation IDs and progress state are memory-only. New request/view disappearance invalidates late delivery; no new persistence is created | **Approve plan** and **Reject** remain disabled until a complete nonempty document is ready. Empty/over-complex plans cannot be approved from the island; **Continue in Claude** remains available, as do timeout/overflow fallback paths | Implemented locally with real non-main-thread, latest-wins, 512-block, pre-render keyboard and combining-grapheme byte regressions; real vendor/unlocked large-plan/VoiceOver gate pending |
| Local SQLite | Task/session ID, source, title, status, phase, timestamps, task URL, waiting message; Manus progress event type/message. Stored source is capped at 64 bytes, task ID/type at 256 bytes, title/phase at 1 KiB, and URL/waiting/progress message at 16 KiB | `~/Library/Application Support/island-app/tasks.sqlite`; optional read-only **View History** sheet. The App-owned final directory is owner-only `0700`; the main database and existing WAL/SHM/journal sidecars are owner-only `0600`. Symlink/non-regular/wrong-owner/multi-link entries fail before SQLite access. No-follow descriptors anchor both final-directory and main-file device+inode for the full Connection lifetime; sidecars are inspected and tightened with directory-relative `fstatat/openat/fchmod`, so a replaced path is not followed | Automatic finite retention of the newest 5,000 task rows and 20,000 progress rows, applied deterministically when the database opens and after every write; deleting an evicted task also deletes its orphaned progress. New writes reject invalid rows atomically; opening an existing database removes wrong-type/oversized rows once. The anchored directory, main file, and sidecars are rechecked before and after every history read, write, retention pass, and Clear History transaction; drift before commit rolls back the write, and drift after a read discards the materialized page | Search up to 200 recent snapshots locally; the query repeats SQLite type and BLOB-byte predicates before SQLite.swift materializes text, so a row modified after open cannot inject oversized UI content. **Settings → Support → Clear History** or manual database removal can delete sooner; destructive confirmation clears task + progress rows in one transaction; history never enters the live attention queue and active in-memory sessions stay visible | Available; the first runtime file-boundary failure terminally disables the active history-store instance, so later calls remain unavailable instead of reporting empty history or a successful clear. Local Hook listening and live in-memory sessions continue |
| Local Codex usage insight | Provider-authored used percentages, window minutes, reset timestamps, event timestamp; the decoder has no prompt/response/path/account/session fields and returns none | On-device scan of at most 8,192 entries under `~/.codex/{sessions,archived_sessions}` while retaining only the newest 24 rollout candidates; each file uses a no-follow descriptor, current-user/type/nonwritable-mode validation, and exact `pread` of the initial at-most-512-KiB suffix; no network or Keychain | In memory only while Settings is open/enabled; concurrent file growth cannot expand the measured read, candidate paths/content are not retained, and no Dev Island database or diagnostics copy is made | **Local Usage Insights** is off by default; enable, refresh, or disable in Settings | Implemented locally for Codex; enumeration overflow, unsafe/replaced files, shrink/read failure and malformed data fail only this optional insight; visual acceptance pending |
| Manus API | Bounded printable-ASCII API key; bounded opaque task IDs; stop command; returned titles, states, timestamps and exact Manus task destinations | Exact `https://api.manus.im` origin through a no-redirect ephemeral session with no Cookie/cache persistence and bounded timeouts | Keychain account `manus_api_key`; current-generation normalized tasks/progress in SQLite keyed by `(source,id)`; late results after stop/reconnect are discarded | Latest Connect attempt owns persistence; Disconnect stops network and removes live Manus tasks, and only reports Not Configured after Keychain deletion succeeds. Unsafe credentials/IDs fail before transport; cross-origin or over-1-MiB responses, over-1000 task lists, unbounded display fields, mismatched response IDs and any task URL other than exact `https://manus.im/app/<same-id>` fail before TaskStore/SQLite. Poll rows are source-scoped and duplicate composite identities coalesce; a Manus ID can never overwrite the same ID from a local Agent. Unauthorized response marks the key invalid and Retry-After is capped at 300 seconds | 60-second actor-isolated polling available |
| Manus public realtime | Bounded printable-ASCII API key for authenticated public-key lookup; exact lowercase `https://<id>.trycloudflare.com/webhook` callback; bounded webhook ID; signed event body; bounded event/task/title/message/attachment metadata | Exact `https://api.manus.ai` origin through the same no-redirect ephemeral session + Cloudflare quick tunnel + local port `7823`. Before tunnel startup or registration, a random private loopback challenge proves this process owns the listener; the redirect/proxy/Cookie/cache-free probe streams only the exact declared body up to 256 bytes. The tunnel executable is resolved in-process from reviewed absolute PATH entries; no `which` helper is run. Startup stderr is drained on a dedicated reader with a 1 MiB ceiling, then continuously discarded without logging | Public key cached in memory for at most one hour; temporary webhook ID in preferences only while listener + process + registration are jointly healthy. The replay window keeps at most 1,024 bounded event IDs in memory until each authenticated signature's exact expiry, extending on a newer signed retry; it is never written to preferences, SQLite, logs or diagnostics. Readiness challenges and cloudflared output are never persisted | Non-Cloudflare callbacks, alternate paths/query/fragment, unsafe IDs, unbounded signed fields, nonmatching Manus task URLs and response-origin drift fail closed before delivery. A full live replay window returns 503 for a new ID instead of evicting a still-replayable ID; duplicates return 200 without delivery. Port conflict, missing/oversized/redirected challenge, silent/cancelled/over-output/early-exit startup, or heartbeat listener loss tears down the public transport and falls back to polling-only; process stop escalates from bounded SIGTERM grace to SIGKILL. Webhook create/stop applies only to `TaskIdentity(source: "manus", id: …)`; a same-ID local session is preserved, a missed Manus create can be recovered alongside it, and Completed/Failed cannot regress to Waiting through a later stopped event. Release gate remains off until a real account passes signed test delivery, lifecycle events and deletion | Official v2 contract implemented; Release-disabled / fail closed; listener ownership, lifecycle, bounded subprocess, transport, signature, replay-saturation, terminal-state, content and composite-identity boundaries covered by deterministic tests, including RSA-signed requests through a live Hummingbird loopback route |
| Maintainer Manus live-acceptance evidence | Four fixed preamble lines, 11 low-cardinality lifecycle/cleanup checkpoints, one allowlisted result, product version, baseline commit, dirty/clean state, UTC bounds, build/exit/validation states, CLI-binary SHA-256, all IslandCore/CLI local-input hashes, exact Package.resolved checkout revisions, and Swift/Xcode/SDK fingerprints. The API key, provider IDs, callback URL, payload, task content and raw errors are excluded | Explicit source-tree wrapper; Manus/Cloudflare only after the maintainer enters a key through the CLI's interactive TTY. Evidence and force-resolved SwiftPM state stay under `/Volumes/T7 Shield/MacMini/CodexFiles/DevIsland-Optimization/`; checkout verification and CLI execution use minimal `env -i` environments | A random `0700` append-never run directory retains the reviewed CLI binary, allowlisted transcript, before/after build-input and toolchain manifests, metadata and top-level checksums until the maintainer deletes it. A transcript outside the allowlist is removed and replaced by a fixed marker; credentials are never persisted | Maintainer explicitly runs `scripts/qa/run-manus-live-acceptance.sh`; Ctrl+C/TERM, timeout, failure and manual review retain non-accepted evidence. Any local/dependency/toolchain drift fails before CLI execution; only CLI exit 0 plus accepted validation creates `ACCEPTED` | Harness and offline attack fixtures available; full local/dependency closure is mechanically bound, but no real-account `ACCEPTED` evidence exists yet, so public realtime remains Release-disabled |
| Sparkle updates | App version/update request and ordinary network metadata; no system profile requested | GitHub Releases appcast and archive | Sparkle preferences/cache under macOS control | Disable automatic checks; manual check | Keyless builds inert; production key/update gate open |
| macOS notifications and signal sounds | Agent display name, task title/phase/waiting message, source + session ID routing metadata; one bundled semantic sound filename | macOS Notification Center | macOS-controlled notification history; sound preference in local UserDefaults | Grant/deny permission; per-category switches; preview or mute all signal sounds | Available |
| Unified logs | Low-cardinality provider/Agent names, event/error categories, HTTP status codes, aggregate counts and retry timing | Apple unified logging on the Mac | macOS-controlled | OS logging controls | Available; API keys, task/session/webhook IDs, URLs, filesystem paths, external-process output, raw bodies and raw error descriptions are prohibited by CI |
| Maintainer PR CI diagnostics | Repository/run identifiers, runner OS/architecture, 11 fixed gate outcomes, first-failure reproduction, aggregate XCTest totals, sanitized failed-case names, completed security gate names, error-marker count, and bounded input `sourceStatus` | GitHub job summary on every CI run; sanitized GitHub Actions artifact only when the job fails | Job-log retention is GitHub-controlled; the two-file diagnostic artifact is retained for 14 days. Runner-temporary raw logs are never included in the artifact | Maintainer workflow only; no in-app collection or user setting. Checkout does not persist the GitHub token for repository scripts | Available in source/PR CI. Raw security/test inputs are descriptor-validated and bounded to 2/16 MiB; unsafe, linked, oversized or changed input becomes unavailable metadata rather than copied content |
| Maintainer authoritative test ownership | No content: one fixed zero-byte lock file and an in-process BSD advisory lock; no PID, command, test name, GitHub context, Secret, App state or user data | The current checkout's `.build/tests-authoritative.lock` only; no network or App destination | The ordinary file persists as current-user `0600`, single-link and empty to preserve lock ordering. The advisory lock exists only while descriptor 9 is open and releases on wrapper exit | Maintainer PR/tag wrapper or explicit local reproduction only. A second run fails immediately before Swift and never terminates the current owner | Available; symlink, non-regular, wrong-owner, wrong-mode, hard-link or nonempty lock state fails closed, and the full suite plus 65 XCTest repetitions and 10 hermetic-listener CLI repetitions share one ownership interval |
| Maintainer workflow shell validation | Checked-in CI/Release YAML, bounded AST structure, workflow/job `defaults.run.shell`, step shell, Bash `run` text, and low-cardinality job/step/line failure coordinates; complete GitHub expressions are replaced by one fixed inert token. No Secret value or installed-App data is read | On-device or GitHub runner bounded Psych AST + safe-YAML parser and `/bin/bash -n`; no shell step is executed and no network destination is used | Workflow bytes exist only for the validation process; attack fixtures use a private temporary directory removed on exit. No artifact or user-data record is created | Maintainer CI/tag workflow only; effective shell resolves step > job default > workflow default > macOS Bash, and every Bash step must pass before either path resolves dependencies; the tag path also passes before credential loading | Available. Workflow input is descriptor-validated and capped at 1 MiB; each run step is capped at 256 KiB. Symlink/hard-link/writable/changed files, multi-document or ambiguous YAML, duplicate/resolved-colliding mapping keys, malformed defaults, shell arguments/templates, unsupported shells, incomplete expressions and invalid Bash syntax fail closed |
| Maintainer repository script syntax validation | Names, metadata and frozen bytes of every `.sh` / `.rb` / `.swift` below repository `scripts/`; low-cardinality relative path only on failure. No repository script is executed and no installed-App data or Secret is read | On-device or GitHub runner descriptor-backed walker followed by minimal-environment stdin-only `/bin/bash -n`, `/usr/bin/ruby -c`, or `/usr/bin/swiftc -parse -`; no network destination | Bytes exist only for the validation process. Positive side-effect markers and negative fixtures stay in a private temporary directory removed on exit; no product/user artifact is created | Maintainer CI/tag workflow only; both workflows validate the current 43 Bash + 15 Ruby + 5 Swift closure before dependency resolution, and tag Release does so before credential loading | Available. Tree/script count, nesting-by-entry, 1-MiB file size, owner/type/mode/nlink/executable/shebang/UTF-8/NUL and pre/post metadata are bounded; file/directory symlink, hard link, partial Bash/Ruby/Swift syntax and concurrent directory drift fail closed |
| Maintainer build-flavor executable inspection | Six fixed low-cardinality build markers: three Performance QA literals and three DEBUG-only literals. No raw executable strings, match context, source, installed-App data, task/session content, path, Keychain value, Agent configuration or Secret is collected | `/usr/bin/strings` against only the newly built `IslandApp` executable on the local Mac or CI runner, followed by an in-process fixed-string comparison; no network or installed-App destination | Raw strings are streamed only to the fixed comparisons and are not logged or retained. Self-test Apps/binaries live in a private temporary directory removed on exit; ordinary QA bundles remain only where the maintainer selected the build output | Maintainer App build, CI/tag gate or explicit local verification only. Production, Performance QA and Debug each have a closed allow/require matrix; the App itself never runs this scan | Available. Every App build checks the lipo'd executable; pair mode also verifies production/Performance Bundle IDs and the plist fixture marker, while 18 negative fixtures reject each missing or leaked marker |
| Maintainer external SwiftPM App scratch | Public pinned dependency checkouts, SwiftPM artifacts, build databases, object files and generated compiler intermediates for exactly one fixed `app-production`, `app-debug`, or `app-performance-qa` flavor; no Agent configuration, Hook authorization, Codex trust, Keychain, SQLite, task/session content or credential is read | A maintainer-selected local directory, normally under `/Volumes/T7 Shield/MacMini/CodexFiles/DevIsland-Optimization/`; no network destination beyond SwiftPM's already pinned dependency acquisition | Build cache persists until the maintainer deletes it. The flavor directory is current-user-owned, non-symlink and non-group/other-writable; a missing final child is created `0700` under one pre-existing validated parent | `DEV_ISLAND_SWIFT_SCRATCH_ROOT` is an explicit build-only override. Omission keeps CI/tag on repository `.build`; the shipping App never reads the setting | Available for maintainer QA. The boundary rejects repository/ancestor/source roots, bare `.build`, unsafe or linked directories and missing multi-level parents before SwiftPM. It avoids File Provider `dataless` stalls but does not change source identity, `Package.resolved`, build flavor, test ownership or release signing gates |
| Maintainer hermetic App launch and performance sampling | QA-only scenario, process ID, Bundle ID/version, matching selected/private executable SHA-256, machine model/architecture, macOS and display-session classification, readiness uptime, bounded one-second CPU/RSS samples, normal-termination state and exit status. No task/session text, credential, Keychain value, Agent configuration, user path or crash report is collected | Only an explicitly selected or just-built `app.devisland.Island.PerformanceQA` Bundle may be input. Its executable/plist are descriptor-hashed and strict-signature checked, then `ditto` copies the Bundle into a random private `0700` sampler root. Only that private copy is dependency/marker verified and launched with a private `CFFIXED_USER_HOME`; `ps` samples the exact PID and `NSRunningApplication` terminates it | The private and selected executable/plist identities are rebound before launch and after normal termination; source replacement fails and publishes no summary. CSV, low-cardinality App marker log and summary are atomically claimed as one append-never set in one safe parent and retained on three fixed current-user `0600` single-link descriptors. Device/inode tokens are rebound through completion; readiness parsing receives only a bounded no-follow/nonblocking App-log snapshot bound to descriptor 8, and the analyzer receives the equivalent CSV snapshot. PR CI retains the public set only in runner temp, while its acceptance flags/hashes come from one bounded sampler-stdout shell variable; it never reopens the public summary after producer exit and excludes all raw files from the failed-run diagnostic artifact. Local evidence stays where the maintainer selected until deletion | Maintainer-only sampler. PR CI always runs an 8-sample idle survival smoke and asserts `isolated_app_snapshot=true` plus equal selected/private hashes from in-process output; locked/unknown override is permitted only for harness integration and remains explicitly invalid for product/Release performance claims | Available. Post-exit public-summary replacement, App-input symlink/FIFO/hash drift, selected-source replacement, existing/symlink/concurrent/replaced/FIFO outputs, malformed or duplicate readiness, reverse/out-of-window uptime, early exit, sample, normal-termination and status-zero failures close the gate. The smoke proves the frozen hermetic artifact launches and exits normally, not production TaskStore, notarized delivery, unlocked presentation, VoiceOver or cross-machine performance |
| Maintainer Production App hermetic launch smoke | Exact fixed launch-profile labels, process ID, Production Bundle ID/version, matching selected/private executable SHA-256, machine/OS/display classification, one monotonic readiness value, eight bounded CPU/RSS survival samples, isolation booleans and normal exit status. No task/session text, Keychain value, Hook/Agent configuration, credential, user path, notification history, crash report, socket destination or payload is collected | Only an explicitly selected `app.devisland.Island` Bundle without a Performance marker. Its executable/plist are descriptor-hashed and strict-signature checked, then the Bundle is copied into the sampler's random private `0700` root. The private copy receives the exact argument/environment opt-in and a private `CFFIXED_USER_HOME`; the real island and status item render while TaskStore bootstrap, LaunchHealth, notifications, Welcome and Sparkle stay inert. `ps`, `lsof` and exact-PID `NSRunningApplication.terminate()` remain on-device | The same three current-user `0600` single-link CSV/App-log/summary descriptors and source/private identity rebinding apply. Readiness and every one-second sample verify no network socket and no isolated-home SQLite or Hook-authorization file; exit rechecks absent product state. PR/tag acceptance is captured only from bounded stdout into shell memory. Runner evidence remains temporary; maintainer evidence remains only at the chosen local path until deletion | Maintainer-only `production-launch-smoke`, fixed to 0 warmup, 8 samples and no performance thresholds. PR CI runs it after Production build; tag Release repeats it after App notarization and before DMG packaging. The paired opt-in is not reached by ordinary Finder/LaunchServices starts | Available as a production-artifact survival/isolation gate. Missing/duplicate/lookalike opt-in, wrong Bundle/marker/flavor, readiness failure, source/private drift, product-state creation, any observed network socket, early exit or nonzero/non-AppKit termination fails closed. Locked/unknown sessions are explicitly invalid for performance, smoothness, first-frame, energy or Release claims; real Agent, user-state migration, unlocked visual and VoiceOver acceptance remain separate gates |
| Bundled Privacy / Terms reading | The exact current bilingual `PRIVACY.md` and `TERMS.md` engineering-review text, including review dates and support addresses; no task, session, path, credential, preference, account or other user data | The signed App's local Settings sheet from `Contents/Resources/Legal`; no fetch, browser, analytics or remote destination. Only an explicit reviewed `mailto:` address or `https://devisland.app` link can leave the sheet after a click | Read-only bytes persist as signed App resources for that App version. Runtime reading is descriptor-backed, no-follow and bounded to 1–512 KiB; the descriptor and path identity/metadata are revalidated after reading. The viewer holds only the selected parsed section in memory while open and creates no preference, history, cache or acceptance record | **Privacy & Support → Privacy Notice / Terms of Use**. The user chooses whether to open, select or follow an allowed link; any link, mode, size or replacement failure remains a fixed local reinstall message | Engineering implementation available. Canonical sources and bundled copies are bilingual structure/date checked and byte-identical; 10 source/bundle plus 6 runtime descriptor boundary cases pass. The visible copies remain explicit owner/legal-review drafts, not published commercial policy or consent evidence |
| Copy / Save Diagnostics | App/build, OS, architecture, key configured state, Manus connection state, local listener health, aggregate status/agent counts | macOS clipboard or a local text file at the location chosen by the user; no upload destination | Clipboard: until replaced. File: until the user deletes or replaces it. Operation and feedback IDs are memory-only and disappear with the Support surface | Explicit **Copy** or **Save…** action; the user controls the local file destination and retention. Cancelling Save releases the surface immediately; an already-started atomic write may finish only at the confirmed destination | Available. Hook inspection and descriptor-backed write/fsync/rename run through one on-device detached executor; only the bounded report or low-cardinality outcome returns. Leaving Support rejects late report/panel/write delivery, and MainActor-to-non-main diagnostic execution is covered by a real-thread regression |
| Previous launch health | Two Booleans, schema version `2`, and a consecutive startup-interruption count capped at `3`; no timestamp, crash report, stack, path, task or user content | Local `UserDefaults` keys `devIsland.launchHealth.didStart`, `devIsland.launchHealth.startupReady`, `devIsland.launchHealth.schemaVersion`, and `devIsland.launchHealth.consecutiveStartupInterruptions`; optional aggregate label in Copy / Save Diagnostics | Count resets when the island and menu-bar surfaces survive the two-second startup window or the user performs a normal Quit; otherwise retained until preferences are reset or removed | Visible only in **Privacy & Support** after a v2 launch failed to reach ready; repeated events add a local recovery suggestion, but never disable Agent listening or mutate Hooks | Available; old `devIsland.launchHealth.cleanExit` is migration-only and an ambiguous false/missing value stays silent; Force Quit/power loss/restart are never called a crash |
| Launch at Login | App registration state | macOS Service Management | macOS-controlled | Settings toggle | Available |
| Jump back | Source app activation or a policy-approved task destination | Local app; exact `https://manus.im/app/<same-task-id>`; or an existing ordinary local directory opened in Finder | Destination may retain normal history | Explicit task click | Launch Services is never called for custom schemes, cross-origin/userinfo/port/query/fragment Manus URLs, remote file URLs, regular files, App/Bundle packages, missing paths or ambiguous bare IDs |
| Future commercial activation and license document | User-entered 16–128 byte activation code; opaque license UUID, positive generation, tier, feature codes, validity timestamps, key ID and signature; no customer PII, payment data, email, account or device ID in the client contract | No shipping destination. A future injected provider transport may receive the code; an untrusted response then passes through the in-process Ed25519 verifier and device-local Keychain account `commercial_license_v1` | Accepted code bytes live in one bounded dedicated allocation shared by value copies and erased with `memset_s` on final release; verified document bytes only use `WhenUnlockedThisDeviceOnly`, explicitly non-synchronizable storage, maximum 32 KiB | No commercial control or activation UI is exposed; explicit cancel/new activation invalidates late responses, validity is checked at commit, exact bytes must pass verify-before-save, same-license generations cannot roll back, and delete is explicit | Provider-neutral activation actor + verifier + disconnected storage implemented; App has zero production trust anchors, endpoint, provider transport or runtime instantiation, so commercial mode remains release-disabled |
| Website/download | IP, time, user agent, requested page or asset | Website host/CDN; GitHub for downloads | Provider-controlled | Visit/download choice | External to the app |
| Support email | Sender address, headers, and user-selected content | Sender/recipient mail providers | Provider/mailbox-controlled | Explicit user action | External to the app |

## Source map

- Local hooks and payload bound:
  `IslandCore/Sources/IslandCore/LocalHooks/LocalHookServer.swift` and
  `IslandCore/Sources/IslandCore/LocalHooks/LocalHookAuthorization.swift`
- Connector registry and managed Hook configuration:
  `IslandCore/Sources/IslandCore/Connectors/Framework/`, including the shared
  descriptor-backed `ManagedConfigFile` transaction boundary for JSON, TOML,
  and standalone plugin configuration; Settings-wide operation projection:
  `IslandAppLib/Presentation/LocalAgentConnectionsPresentation.swift`
- Explicit local live-readiness preflight and listener challenges:
  `IslandCore/Sources/IslandCore/Connectors/Framework/LocalLiveReadiness.swift`
  and `IslandCore/Sources/IslandCore/LocalHooks/HermeticLocalListenerReadiness.swift`
  and `IslandCoreCLI/Sources/IslandCoreCLI/main.swift`
- Bounded local helper execution and precise tmux navigation:
  `IslandCore/Sources/IslandCore/Internal/BoundedChildProcess.swift` and
  `IslandCore/Sources/IslandCore/Internal/TmuxSessionNavigator.swift`
- Codex integration modes, bounded App Server trust probe, and unsupported
  cloud-monitoring boundary:
  `IslandCore/Sources/IslandCore/Connectors/Codex/CodexHookTrustProbe.swift`
  and `docs/codex-integration-field-notes.md`
- Gemini CLI payload mapping and pinned contract:
  `IslandCore/Sources/IslandCore/Connectors/GeminiCLI/` and
  `docs/gemini-cli-hooks-notes.md`
- Qwen Code payload mapping and pinned contract:
  `IslandCore/Sources/IslandCore/Connectors/QwenCode/` and
  `docs/qwen-code-hooks-notes.md`
- GitHub Copilot CLI payload mapping and pinned contract:
  `IslandCore/Sources/IslandCore/Connectors/CopilotCLI/` and
  `docs/copilot-cli-hooks-notes.md`
- Kimi Code CLI payload mapping, lossless TOML maintenance and pinned contract:
  `IslandCore/Sources/IslandCore/Connectors/KimiCode/`,
  `IslandCore/Sources/IslandCore/Connectors/TomlHookConfigEditor.swift`, and
  `docs/kimi-code-hooks-notes.md`
- OpenCode privacy-minimal plugin, standalone-file ownership and pinned contract:
  `IslandCore/Sources/IslandCore/Connectors/OpenCode/`,
  `IslandCore/Sources/IslandCore/Connectors/Framework/StandalonePluginFileEditor.swift`,
  and `docs/opencode-plugin-notes.md`
- Claude/Codex/Qwen approval, Claude questions, and Claude plan review:
  `IslandCore/Sources/IslandCore/Connectors/ClaudeCode/`,
  `IslandCore/Sources/IslandCore/Connectors/Codex/`, and
  `IslandCore/Sources/IslandCore/Models/AgentActionRequest.swift`; Plan Review
  background rendering and bounded SwiftUI document:
  `IslandAppLib/Presentation/PlanMarkdownRendering.swift`
- Local task storage:
  `IslandCore/Sources/IslandCore/Storage/SQLiteStore.swift`
- API-key storage:
  `IslandCore/Sources/IslandCore/Storage/KeychainStore.swift`
- Manus requests and realtime gate:
  `IslandCore/Sources/IslandCore/Manus/`,
  `IslandCore/Sources/IslandCore/Tunnel/`, and
  `IslandCore/Sources/IslandCore/TaskStore.swift`
- Maintainer Manus acceptance evidence:
  `scripts/qa/run-manus-live-acceptance.sh` and
  `scripts/qa/validate-manus-live-acceptance-transcript.rb`, plus
  `scripts/qa/generate-manus-live-build-inputs.rb`
- Returned-content and Launch Services destination boundary:
  `IslandCore/Sources/IslandCore/Manus/ManusRemoteContentPolicy.swift` and
  `IslandCore/Sources/IslandCore/Internal/TaskDestinationPolicy.swift`
- Composite task identity and source-owned snapshot merge:
  `IslandCore/Sources/IslandCore/Sync/StateReconciler.swift`
- Updates: `IslandAppLib/Updates/AppUpdateController.swift` and
  `scripts/build-app.sh`
- Notifications: `IslandAppLib/Notifications/TaskNotifier.swift`
- Diagnostics and privacy-minimal launch health:
  `IslandAppLib/Support/SupportDiagnostics.swift`,
  `IslandAppLib/Support/SupportDiagnosticsExporter.swift`, and
  `IslandAppLib/Support/LaunchHealthTracker.swift`; design and recovery boundary:
  `docs/LAUNCH_HEALTH.md`
- Local usage: `IslandCore/Sources/IslandCore/Usage/CodexLocalUsageReader.swift`,
  `IslandCore/Sources/IslandCore/Models/AgentUsageSnapshot.swift`, and
  `IslandAppLib/Usage/AgentUsageController.swift`
- Commercial activation, license verification, verify-before-save storage, and
  threat model:
  `IslandCore/Sources/IslandCore/Commerce/CommercialLicenseActivation.swift`,
  `IslandCore/Sources/IslandCore/Commerce/CommercialLicenseVerifier.swift`,
  `IslandCore/Sources/IslandCore/Commerce/CommercialLicenseDocumentStore.swift`,
  `docs/COMMERCIAL_LICENSE_SECURITY.md`, and
  `docs/COMMERCIAL_ACTIVATION_THREAT_MODEL.md`
- Maintainer PR CI diagnostics:
  `.github/workflows/ci.yml`, `scripts/ci/generate-ci-diagnostics.rb`,
  `scripts/ci/verify-ci-diagnostics.sh`, and `docs/CI_DIAGNOSTICS.md`
- Maintainer workflow run-shell validation:
  `scripts/release/verify-workflow-run-shells.rb` and
  `scripts/ci/verify-workflow-shell-syntax.sh`
- Maintainer repository script syntax validation:
  `scripts/release/verify-repository-script-syntax.rb` and
  `scripts/ci/verify-repository-script-syntax.sh`
- Build-flavor executable marker inspection:
  `scripts/ci/verify-performance-fixture-isolation.sh`, invoked by
  `scripts/build-app.sh` after lipo and by `scripts/ci/verify-security-invariants.sh`
- Offline legal-document source/bundle boundary and Settings viewer:
  `scripts/release/verify-legal-documents.rb`, `scripts/build-app.sh`,
  `IslandAppLib/Support/LegalDocument.swift`, and
  `IslandAppLib/Views/Settings/SettingsView.swift`

## Release invariant

Any change that introduces a destination, data category, persistence location,
diagnostic field, analytics/crash service, payment/licensing service, account,
or public Webhook must update this inventory and `PRIVACY.md` before release.
